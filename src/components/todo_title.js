import React,{Component} from 'React';

const Title = ({todoCount}) => {
  return (
    <div>
       <div style={{width:'1000'}}>
          <h1 className="headtodo">ToDo List</h1>
       </div>
    </div>
  );
}
export default Title;
